pipeline {
    agent any

    stages {
        stage('From GitHub') {
            steps {
                echo 'Pipeline running from GitHub 🎉'
                sh 'date'
                sh 'whoami'
            }
        }
    }
}
